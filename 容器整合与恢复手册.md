# 容器整合与恢复手册

> 日期：2026-09-15　主机：192.168.1.81（本机，colima VM）
> 目标：梳理重复功能容器、共用资源、缓解内存压力。

## 一、最终状态

- 运行容器：**26 → 19**
- colima VM 宿主 RSS：**~5.3GB → 2.50GB**（本会话回收约 2.8GB，相对满载 26 容器时更多）
- VM 客户机 used：~6.76GB → 5.0GB
- 健康核查：Dify api `healthy`、Dify Web `307`、KGE Elasticsearch `200`

### 仍在运行（19）
- **Dify 全栈（15）**：api / worker / worker_beat / api_websocket / web / plugin_daemon / agent_backend / db_postgres(PG15) / redis(v6) / weaviate / sandbox / local_sandbox / nginx / ssrf_proxy / agent_ssrf_proxy
- **KGE 基础设施（4）**：dev-elasticsearch / dev-postgres(PG16) / dev-redis(v7) / dev-minio

## 二、本次做了什么

| # | 动作 | 回收 | 可逆性 |
|---|---|---|---|
| 1 | 停用 deer-flow 栈（frontend/gateway/nginx/redis）| ~861M | `docker start` 秒恢复 |
| 1 | 停用 dev-kkfileview（文件预览）| ~718M | `docker start` 秒恢复 |
| 1 | 停用 dev-unstructured（Dify ETL 解析后端）| ~303M | `docker start` 秒恢复 |
| 2 | dev-elasticsearch JVM 堆 `1g→512m`（数据 <1MB）| ~445M | 改回 compose 重建 |
| 4 | 停用 dev-mysql（`dev_db` 为空、无数据）| ~355M | `docker start` 秒恢复 |

**唯一的配置文件改动**：`/Users/hjx/Documents/03_Resource/开发环境/docker-compose.yml` 中
`ES_JAVA_OPTS: "-Xms1g -Xmx1g"` → `"-Xms512m -Xmx512m"`。

**备份位置**（改配前的原始文件）：
`/Users/hjx/Documents/01_Project/knowledge-governance-expert/.consolidate-backup-20260915-010052/`
- `dev-services-docker-compose.yml.bak`
- `dev-services.env.bak`
- `dify.env.bak`

## 三、恢复手册（按需一键启动）

```bash
# Dify 上传/解析新文档失败时（ETL 依赖 unstructured）
docker start dev-unstructured

# 需要文件预览（kkFileView，:8012）
docker start dev-kkfileview

# 需要 deer-flow 问答 sidecar（百晓生 RAG 问答链路）
docker start deer-flow-nginx deer-flow-redis deer-flow-gateway deer-flow-frontend

# 需要本地 MySQL（一般不需要：XXL-Job 用的是独立的 kge-xxl-job-mysql）
docker start dev-mysql
```

> 说明：`kge-xxl-job-admin` / `kge-xxl-job-mysql` 是 XXL-Job 自己的容器，本次整合前就已停止
> （`XXL_JOB_ENABLED=false`），与本手册的 dev-mysql 无关。

## 四、评估后放弃/跳过的项（附原因）

| 项 | 结论 | 原因 |
|---|---|---|
| **weaviate → dev-elasticsearch** | **中止** | Dify 无「切换向量库后端后自动重建」能力；切换后 8 个知识库/135 篇文档检索会失效，只能逐库手动重嵌（走 SiliconFlow 付费 API，且现存 23 篇 error/6 篇 waiting 大概率再失败）。这 8 个库是「杰克百晓生」问答知识源，为省 287M 打瘫线上问答不划算。 |
| **Redis 3→1 合并** | **跳过** | 三实例各仅 ~5M，合并省 ~10M；但 Dify(密码 difyai123456/DB0-1) 与 KGE(dev123456/DB0) 密码不同、DB0 撞 key，需统一密码+重映射 DB+跨网络+重建 Dify 重容器，ROI 极差。 |
| **PG / ES / Redis 跨机迁到 192.168.1.254** | **不做** | 本机→.254 延迟 avg 43ms / max 70ms；PG、Celery broker、ES 检索均延迟敏感，跨机会明显拖慢 Dify 与 KGE。 |

## 五、192.168.1.254（jack 共享主机）现状

- LAN 可达且开放：PG16(5432) / MySQL8.4(3306) / MinIO(9000-1) / Xinference(9997) / RAGFlow(9380) / ES(9200) / Redis(6379)
- **PG16 可用** `postgres` 超级用户远程登录成功（现有库仅 `appdb`），需要时可远程建库
- **ES(9200) / Redis(6379) 有鉴权**，CREDENTIALS.md 未记录账号密码 → 当前无法共用
- **SSH `jack@192.168.1.254` 被拒**（publickey/password）→ 无法在 .254 上安装新资源
- 凭据来源：项目内 `CREDENTIALS.md`

## 六、重要提醒：宿主机 swap 的真因

宿主机 48G 内存中 **colima VM 仅占 ~2.5–5G，其余 ~42G 是 Mac 上其他程序**。
整合容器**无法解决 swap 92% 的问题**——若仍卡顿，需关闭其他吃内存的应用（浏览器/IDE 等）。
内存压力过高时 docker CLI 会挂起（`docker stats` 超时即此因）。

## 七、后续可选（需你提供前置条件）

1. 想用 .254 的 ES/Redis 做共用 → 需提供 .254 的 ES 账号密码 + Redis 密码，或 SSH 访问。
2. XXL-Job 启用并共用 .254 MySQL → 需在 .254 MySQL 建 `xxl_job` 库并导入 `deploy/xxl-job/tables_xxl_job.sql`。
3. 若坚持 weaviate→ES → 建议先在 Dify UI 逐库确认 embedding 可用，并接受检索中断窗口 + 重嵌成本。
