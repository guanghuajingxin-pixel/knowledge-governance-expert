"""Pydantic schemas for faq-service.

Route-level request bodies are declared inline in each route module
(matching the Task 10 brief). This module is kept as a placeholder so
the package layout matches the brief's file list.
"""
